# 个人工具箱 — 需求问答上下文总结

> 本次对话起始于用户的模糊想法，通过逐步提问明确了需求，最终基于已有饮水提醒工具（Electron + React），在 `C:\Users\Xtttt\OneDrive\Desktop\project\tools` 下搭建了多功能本地工具箱项目，并实现了待办清单模块。

---

## 一、项目总体方向

**Q1：** 你已有一个饮水提醒工具，为什么想把 todo list、番茄钟、记账助手也加进去？是觉得切换多个独立应用太麻烦，还是这些功能之间有某种关联？  
**A1：** 希望做一个集合所有功能的、仅用于自己的本地工具箱。

**Q2：** 你现有的饮水提醒工具是你自己开发的，还是别人做的？对 Electron + React 这套技术栈熟悉到什么程度？  
**A2：** 用 AI 做的，这个项目也会由 AI 来做。

**Q3：** 这几个功能模块里，你最迫切想用的是哪一个？日常生活里哪个场景最让你觉得"要是有个小工具帮我就好了"？  
**A3：** todo list（待办清单）。

---

## 二、技术实现方向

**Q4（追问）：** 你现在怎么管理待办事项？用纸笔、手机备忘录、还是其他工具？目前的方式让你最不满意的地方是什么？  
**A4：** 手机备忘录。但用户表示 AI 不需要关心这些，只需关心具体项目实现细节，有足够信息后落地实现即可。

---

## 三、Todo List 功能细节

**Q5：** 如何组织任务？简单的"待完成/已完成"两级就够了，还是需要分类、标签、优先级、截止日期等更复杂的维度？  
**A5：** 要有优先级、截止日期、标签。

**Q6：** 截止日期到了或任务过期了，需要什么样的提醒方式？系统通知弹窗、任务标红显示、还是会有声音提示？  
**A6：** 系统通知弹窗。

**Q7：** 数据存储方面，希望完全本地持久化（JSON 文件或 SQLite），还是后续会考虑云同步？  
**A7：** 用 JSON 本地持久化即可。

---

## 四、工具箱整体架构

**Q8：** 多个功能之间如何切换？左侧导航栏、顶部 Tab、还是系统托盘菜单各自打开独立窗口？  
**A8：** 主页中分为一个个小功能组件，点开后进入具体功能页面。左侧导航栏为当前功能的导航栏，顶部有返回箭头，各功能彼此独立可以并行运行。

---

## 五、Todo List 视图与排序

**Q9：** 任务列表视图偏好？列表按优先级排序、看板式（待办/进行中/已完成）、还是日历视图按截止日期排列？  
**A9：** 不需要看板。待办和已完成的任务在列表中顺序接连而下，已完成的任务画上一条横线自动排到所有任务后面。要有排序，排序可以用筛选功能来做，名称、优先级、截止日期等各有一个排序小箭头。

---

## 六、Todo List 左侧导航栏

**Q10：** 在 todo list 功能里，左侧导航栏放什么内容？比如按标签筛选的入口、已完成/未完成的分组视图、还是其他？  
**A10：** 暂时没想好，后续需要再加，可以先空着。

---

## 七、与现有饮水提醒工具的整合方式

**Q11：** 现有饮水提醒工具是直接替换掉做成新的多功能工具箱，还是保留它不动、另起一个新项目？  
**A11：** 希望把饮水提醒加入到工具箱里，饮水提醒工具文件在 `C:\Users\Xtttt\OneDrive\Desktop\project\drinking-counter`。

> 此后 AI 阅读了饮水提醒工具的完整代码，确认了其技术栈：Electron 33 + React 18 + Vite 6 + electron-store + lucide-react，单文件 App.jsx，纯本地数据存储。

---

## 八、Todo List 细节继续深入

**Q12：** 优先级有几个等级？高/中/低三级，还是用 P0-P3 或者颜色标记？  
**A12：** P0-P3。

**Q13：** 标签是预先定义好一组固定的（比如"工作""个人""紧急"），还是可以自由创建和删除任意标签？  
**A13：** 自由创建删除。

**Q14：** 每个 todo 任务除了标题，还需要支持备注/描述文字吗？还是只需要一个标题就够了？  
**A14：** 需要支持描述。

**Q15：** 备注描述怎么查看——列表中点击展开/折叠，还是要弹出一个详情编辑窗口？  
**A15：** 列表中点击展开/折叠。

**Q16：** 任务需不需要支持子任务（将一个任务拆成多个子步骤），还是只需要扁平的单层任务列表？  
**A16：** 需要支持子任务。

**Q17：** 子任务需要有自己的优先级、截止日期和标签吗，还是只是勾选式的 checklist？  
**A17：** checklist 即可。

**Q18：** 子任务有数量限制吗？或者说一个主任务下大概最多会有多少个子步骤？  
**A18：** 最多 8 个。

---

## 九、整体项目结构与首页

**Q19：** 主页面展示各功能小组件卡片，工具箱启动时是直接展示主页，还是希望打开后默认进入上次使用的功能？  
**A19：** 展示主页。

**Q20：** 截止日期的提醒时机——是到期当天提醒、提前几天提醒、还是用户可以自己设定提前多久？  
**A20：** 用户可以自己设置。（支持 0/5/15/30/60/120 分钟、1 天前）

**Q21：** 除了上面聊到的这些，todo list 还有没有什么功能是你觉得"应该有但还没聊到"的？比如搜索、批量操作、数据导出等？  
**A21：** 要有批量删除。

---

## 十、最终落地方案

根据以上全部问答，在 `C:\Users\Xtttt\OneDrive\Desktop\project\tools` 下构建了个人多功能本地工具箱项目，具体实现方案如下：

### 技术栈
| 层级 | 技术 | 版本 |
|------|------|------|
| 桌面壳 | Electron | 33 |
| UI 框架 | React | 18 |
| 打包工具 | Vite | 6 |
| 路由 | react-router-dom (HashRouter) | 6 |
| 持久化存储 | electron-store（双 store 隔离） | 8 |
| 图标库 | lucide-react | 0.468 |

### 项目结构
```
tools/
├── electron/
│   ├── main.js              # 主进程：water-data + todo-data 双 store
│   ├── preload.js           # IPC 桥：window.waterApi + window.todoApi
│   └── assets/              # app.ico / app.png（从旧项目复制）
├── src/
│   ├── main.jsx + App.jsx   # HashRouter 路由：/ /drinking /todo
│   ├── styles.css           # 完整样式（保留原饮水全部样式 + todo 新样式）
│   ├── pages/Home.jsx       # 主页：4 张功能卡片
│   └── modules/
│       ├── DrinkingApp.jsx + drinking/DrinkingView.jsx  # 原饮水逻辑完整移植
│       ├── TodoApp.jsx + todo/TodoView.jsx              # Todo 全部功能 + EditModal
├── package.json             # appId: local.personal.toolbox
├── vite.config.mjs
└── index.html
```

### Todo 功能对照表
| 需求 | 实现 |
|------|------|
| P0-P3 优先级 | 四色编码（红/橙/绿/灰），Flag 图标标识 |
| 截止日期 | `<input type="date">`，逾期标红 + 左边框标红 |
| 系统通知提醒 | 提前量可自设（0分钟~1天前），main 进程 60 秒轮询到期提醒 |
| 自由创建/删除标签 | 底部标签栏 + 输入框新建，点击标签筛选，× 删除 |
| 备注/描述 | 支持，列表中点击展开查看全文，Modal 中可编辑 |
| 子任务 checklist | 最多 8 个，展开/折叠查看，独立勾选 |
| 排序 | 名称/优先级/截止日期 三列排序箭头，升序/降序切换 |
| 筛选 | 搜索框模糊匹配、优先级下拉、标签点击筛选、显示/隐藏已完成 |
| 批量删除 | 复选框多选，工具栏动态显示"删除(N)"按钮 |
| 已完成任务 | 文字删除线 + 自动排到列表末尾 |
| JSON 本地持久化 | 独立 `todo-data` store，与饮水 `water-data` 隔离 |
| 主页 → 功能页 | 点卡片进入，顶部返回箭头回主页 |
| 左侧导航栏占位 | 保留结构，后续扩展 |

### 番茄钟 & 记账助手
主页卡片已预留，标记为"即将推出"（disabled），路由尚未注册。

### 运行命令
```bash
npm install        # 安装依赖（参见下方修复说明）
npm run start      # 开发模式：Vite + Electron 并行
npm run dist       # 打包为 Windows 可执行目录
```

### 已知问题 & 修复
- `node_modules` 因虚拟机软链接在 Windows 下损坏导致 `npm install` 失败：需先删除 `tools/node_modules/`，再执行 `npm install`。
- `vite.config.mjs` 已还原为与原项目一致的简洁写法（移除对 `@vitejs/plugin-react` 的显式 import）。
