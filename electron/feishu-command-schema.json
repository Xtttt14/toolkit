{
  "type": "object",
  "additionalProperties": false,
  "required": ["status", "action", "payload", "message"],
  "properties": {
    "status": { "type": "string", "enum": ["action", "clarify"] },
    "action": { "type": ["string", "null"], "enum": ["add_todo", "add_expense", "add_income", "add_total", "add_water", null] },
    "payload": {
      "type": "object",
      "additionalProperties": false,
      "required": ["title", "description", "dueDate", "priority", "amount", "tag", "note", "date", "name", "ml"],
      "properties": {
        "title": { "type": ["string", "null"] },
        "description": { "type": ["string", "null"] },
        "dueDate": { "type": ["string", "null"] },
        "priority": { "type": ["string", "null"], "enum": ["P0", "P1", "P2", "P3", null] },
        "amount": { "type": ["number", "null"] },
        "tag": { "type": ["string", "null"] },
        "note": { "type": ["string", "null"] },
        "date": { "type": ["string", "null"] },
        "name": { "type": ["string", "null"] },
        "ml": { "type": ["number", "null"] }
      }
    },
    "message": { "type": "string" }
  }
}
